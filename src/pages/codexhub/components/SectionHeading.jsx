import React from "react";

export const SectionHeading = ({ label }) => (
  <div className="cxprofile-section-heading">
    <span className="cxprofile-section-accent" />
    <h2 className="cxprofile-section-label">{label}</h2>
  </div>
);
